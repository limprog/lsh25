Сохранение изменений для коммита:
git add .

Создание коммита:
git commit -m "Название коммита"

Обновление главной ветки:
git push lsh25_remote main

Обновление локальной главной папки и загрузка:
git checkout main
git pull lsh25_remote main

Получение изменений главной ветки:
git pull lsh25_remote main


git add .
git commit -m "New"
git push lsh25_remote vps_server


cd /lsh25
git fetch --all
git reset --hard origin/vps_server  
cd /lsh25/yaml
docker compose down # Остановит и удалит все контейнеры, сети и тома (если они не именованы явно, как у вас)
docker compose up -d --build # Пересобирает образы и запускает все заново

data-task_id="<?php echo $task['_id']['$oid'] ?>"