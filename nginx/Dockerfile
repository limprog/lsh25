# nginx/Dockerfile
FROM nginx:latest

# Копируем ваш пользовательский конфигурационный файл NGINX
# Это самый важный шаг для настройки NGINX под ваши нужды (проксирование, статика и т.д.)
# Убедитесь, что ваш `nginx.conf` (или `default.conf` внутри папки `conf.d`)
# находится рядом с этим Dockerfile в папке `nginx/`.
COPY ./nginx.conf /etc/nginx/nginx.conf

# Опционально: если у вас есть статические файлы (HTML, CSS, JS), которые NGINX должен отдавать
# COPY ./html /usr/share/nginx/html

# Указываем, какие порты NGINX слушает внутри контейнера.
# Это не пробрасывает порты на хост, а лишь информирует Docker.
EXPOSE 80
EXPOSE 443

# NGINX обычно запускается как CMD в базовом образе,
# так что часто эту строку не нужно явно указывать,
# но если нужна кастомная команда, убедитесь, что NGINX остается в фореграунде:
# CMD ["nginx", "-g", "daemon off;"]